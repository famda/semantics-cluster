# Copy this file to .env and fill in the values before running docker compose.
# NEVER commit .env to version control.

# ----------------------------------------------------------------------------
# Tailscale auth key (required on ALL machines — head and workers)
# Generate a reusable key at: https://login.tailscale.com/admin/settings/keys
# Tip: enable "Ephemeral" if you want nodes to auto-deregister on disconnect.
# ----------------------------------------------------------------------------
TS_AUTHKEY=tskey-auth-rest-of-your-key-goes-here

# ----------------------------------------------------------------------------
# HEAD machine only  →  keep TS_HOSTNAME=ray-head (hardcoded in compose)
# WORKER machines    →  set a unique name, e.g. ray-worker-1, ray-worker-2
# ----------------------------------------------------------------------------
# TS_HOSTNAME=ray-worker-1

# ----------------------------------------------------------------------------
# WORKER machines only: address of the head node on the Tailscale network.
# Uses Tailscale MagicDNS so the head hostname resolves automatically.
# Format: <tailscale-hostname-of-head>:<ray-gcs-port>
# ----------------------------------------------------------------------------
RAY_HEAD_ADDRESS=ray-head:6379

# ----------------------------------------------------------------------------
# Grafana admin password (head machine only)
# Used by the grafana service. Change before first run.
# ----------------------------------------------------------------------------
GF_ADMIN_PASSWORD=changeme
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# AZURITE_CONN_STR is the connection string for Azure Storage Emulator (Azurite).
# ----------------------------------------------------------------------------
AZURITE_CONN_STR=DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://localhost:10000/devstoreaccount1;
